application:
  name: familie-ef-sak
server:
  servlet:
    context-path: /
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

no.nav.security.jwt:
  issuer.azuread:
    discoveryurl: ${AZUREAD_DISCOVERY_URL}
    accepted_audience: ${AZURE_CLIENT_ID}
    proxyurl: http://webproxy-nais.nav.no:8088
    cookie_name: azure_token
  client:
    registration:
      familie-integrasjoner:
        resource-url: ${FAMILIE_INTEGRASJONER_URL}
        token-endpoint-url: ${AZUREAD_TOKEN_ENDPOINT_URL}
        grant-type: urn:ietf:params:oauth:grant-type:jwt-bearer
        scope: ${FAMILIE_INTEGRASJONER_SCOPE}
        authentication:
          client-id: ${AZURE_CLIENT_ID}
          client-secret: ${AZURE_CLIENT_SECRET}
          client-auth-method: client_secret_basic
      familie-oppdrag:
        resource-url: ${FAMILIE_OPPDRAG_API_URL}
        token-endpoint-url: ${AZUREAD_TOKEN_ENDPOINT_URL}
        grant-type: client_credentials
        scope: ${FAMILIE_OPPDRAG_SCOPE}
        authentication:
          client-id: ${AZURE_CLIENT_ID}
          client-secret: ${AZURE_CLIENT_SECRET}
          client-auth-method: client_secret_basic
      infotrygd-feed:
        resource-url: ${INFOTRYGD_FEED_API_URL}
        token-endpoint-url: ${AZUREAD_TOKEN_ENDPOINT_URL}
        grant-type: client_credentials
        scope: ${EF_INFOTRYGD_FEED_SCOPE}
        authentication:
          client-id: ${AZURE_CLIENT_ID}
          client-secret: ${AZURE_CLIENT_SECRET}
          client-auth-method: client_secret_basic
# PDL er per nå ikke lagt inn i azureAd, men det skal komme.
#      pdl:
#        resource-url: ${PDL_URL}
#        token-endpoint-url: ${AZUREAD_TOKEN_ENDPOINT_URL}
#        grant-type: urn:ietf:params:oauth:grant-type:jwt-bearer
#        scope: ${PDL_SCOPE}
#        authentication:
#          client-id: ${AZURE_CLIENT_ID}
#          client-secret: ${AZURE_CLIENT_SECRET}
#          client-auth-method: client_secret_basic

spring:
  autoconfigure.exclude: org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration
  data:
    jdbc:
      repositories:
        enabled: true
  main:
    allow-bean-definition-overriding: true
    banner-mode: "off"
  flyway:
    enabled: true
  datasource:
    url: jdbc:postgresql://A01DBVL010.adeo.no:5432/familie-ef-sak
    hikari:
      maximum-pool-size: 2
      connection-test-query: "select 1"
      max-lifetime: 30000
      minimum-idle: 1

management:
  endpoint.health.show-details: always
  endpoints.web:
    exposure.include: info, health, metrics, prometheus
    base-path: "/internal"
    path-mapping:
      info: "status/isAlive"
  metrics.export.prometheus.enabled: true
  metrics.web.server.auto-time-requests: true

AZUREAD_DISCOVERY_URL: https://login.microsoftonline.com/navno.onmicrosoft.com/v2.0/.well-known/openid-configuration
AZUREAD_TOKEN_ENDPOINT_URL: https://login.microsoftonline.com/navno.onmicrosoft.com/oauth2/v2.0/token
FAMILIE_INTEGRASJONER_URL: http://familie-integrasjoner
STS_URL: http://security-token-service.default.svc.nais.local/rest/v1/sts/token?grant_type=client_credentials&scope=openid
PDL_URL: http://pdl-api
FAMILIE_OPPDRAG_API_URL: http://familie-oppdrag
INFOTRYGD_FEED_API_URL: https://familie-ef-infotrygd-feed

FRONTEND_OPPGAVE_URL: https://ensligmorellerfar.prod-fss.nais.io/fagsak

rolle:
  veileder: "31778fd8-3b71-4867-8db6-a81235fbe001"
  saksbehandler: "6406aba2-b930-41d3-a85b-dd13731bc974"
  beslutter: "5fcc0e1d-a4c2-49f0-93dc-27c9fea41e54"